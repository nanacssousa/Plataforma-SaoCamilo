export interface AmbienteLeitura { id_leitura: number; id_local: number | null; id_sessao: number | null; fonte: 'SENSOR' | 'OPENMETEO' | 'MANUAL'; temperatura_c: number; umidade_pct: number; indice_calor_c: number | null; pressao_hpa: number | null; velocidade_vento_ms: number | null; chuva_mm: number | null; dispositivo_id: string | null; lido_em: string; }
export interface LocalTreino { id_local: number; nome: string; latitude: number | null; longitude: number | null; descricao: string | null; ativo: number; criado_em: string; }
export interface SensorPayloadDTO { dispositivo_id: string; id_local?: number; id_sessao?: number; temperatura_c: number; umidade_pct: number; pressao_hpa?: number; }
export interface AmbienteAtualDTO { fonte: 'SENSOR' | 'OPENMETEO' | 'MANUAL'; temperatura_c: number; umidade_pct: number; indice_calor_c: number; pressao_hpa: number | null; velocidade_vento_ms: number | null; chuva_mm: number | null; condicao: 'CONFORTAVEL' | 'ATENCAO' | 'CRITICO'; descricao_condicao: string; lido_em: string; id_leitura: number; }
export interface OpenMeteoResponse { current: { temperature_2m: number; relative_humidity_2m: number; apparent_temperature: number; surface_pressure: number; wind_speed_10m: number; precipitation: number; }; }
